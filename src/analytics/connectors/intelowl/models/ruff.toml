extend = "../../../../../ruff.toml"

[lint]
ignore = [
    "N999",    # Non-snake_case module names
    "TID252",  # Relative imports from parent modules
    "RUF012",  # Mutable class attributes (default values of Pydantic fields)
    # Move import intro type-checking block (validation needs types at runtime).
    "TC001",
    "TC002",
    "TC003",
]
